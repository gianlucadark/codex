---
import { Image } from 'astro:assets';
import logoGd from '../assets/logo-gd-transparent.png';

interface Props {
	alt?: string;
	loading?: 'eager' | 'lazy';
}

const { alt = 'GD', loading = 'eager' } = Astro.props;
---

<Image
	class="logo-gd"
	src={logoGd}
	alt={alt}
	width={84}
	height={84}
	widths={[42, 84, 126]}
	sizes="42px"
	format="webp"
	quality={86}
	loading={loading}
	decoding="async"
/>

<style>
	.logo-gd {
		display: block;
		width: 42px;
		aspect-ratio: 1;
		height: auto;
		object-fit: contain;
	}
</style>
